---
title: Button
package: '@nature-ui/button'
description:
  Button component is used to trigger an action or event, such as submitting a
  form, opening a Dialog, canceling an action, or performing a delete operation.
---

The Button component is used to trigger an action or event, such as submitting a
form, opening a dialog, canceling an action, or performing a delete operation.

## Import

```js
import { Button } from '@nature-ui/core';
```

## Usage

```jsx
<Button color='blue-500'>Button</Button>
```

### Button Sizes

Use the `size` prop to change the size of the button. You can set the value to
`xs`, `sm`, `md`, or `lg`.

```jsx
<Stack spacing={4} direction='row' className='align-center'>
  <Button color='teal-500' size='xs'>
    Button
  </Button>
  <Button color='teal-500' size='sm'>
    Button
  </Button>
  <Button color='teal-500' size='md'>
    Button
  </Button>
  <Button color='teal-500' size='lg'>
    Button
  </Button>
</Stack>
```
